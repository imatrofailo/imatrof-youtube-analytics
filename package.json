{
  "name": "imatrof-youtube-analytics-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18 <25"
  },
  "scripts": {
    "dev": "vite",
    "refresh:site": ".venv/bin/python scripts/weekly_site_refresh_local.py",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.4.3",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "postcss": "^8.5.18"
  }
}